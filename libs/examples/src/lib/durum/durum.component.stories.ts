import { Meta } from '@storybook/angular';
import { DurumComponent } from './durum.component';

export default {
  title: 'Durum',
  component: DurumComponent,
} as Meta<DurumComponent>;

export const Primary = () => ({});
